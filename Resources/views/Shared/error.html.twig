{% if error %}<div class="meta-error">{% set lower = error|lower %}<span class="meta-badge failed">{% if '24 hours' in lower %}Janela de resposta encerrada{% elseif 'invalid whatsapp phone' in lower %}Número inválido{% elseif 'capability' in lower or 'permission' in lower %}Permissão insuficiente na tentativa{% elseif 'opt-in' in lower %}Consentimento não registrado{% elseif 'unconfirmed' in lower or 'integrity correction' in lower %}Entrega não confirmada{% else %}Revisão necessária{% endif %}</span><details><summary>Ver motivo técnico</summary><pre>{{ error }}</pre></details></div>{% else %}—{% endif %}
